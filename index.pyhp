<html>
<head>
<title>ToDoList</title>
<link rel="stylesheet" href="/static/css/styles.css">
</head>
<body>

<h2>ToDoList</h2>

<form method="POST" id="create"></form>
<form method="POST" id="toggle-stat"></form>

<table>
<tr>
  <th>ToDo<button>↓</button></th>
  <th>Deadline<button>↓</button></th>
  <th><button>👀</button></th>
</tr>
{
def item(name,deadline,status):
  if status:
    return f'<tr><td>{name}</td><td>{deadline}</td><td><button>✓</button></td></tr>'
  else:
    return ''
for i in db.read():
  ret += item(**i)
}
<tr>
  <td><input form="create" name="name" type="text" value="aaa"></td>
  <td><input form="create" name="deadline" type="date" value="2023-05-30"></td>
  <td><button form="create" name="type" value="create" type="submit">＋</button></td>
</tr>
</table>

<h2>Sample Script</h2>

<p>{ret='Hello World'}</p>

<pre>
i = 2; ret += str(i) >> {i = 2; ret += str(i)}
i += 2; ret += str(i) >> {i += 2; ret += str(i)}
</pre>

<pre>
import numpy as np
ret = str(np.mean(range(10)))
>> {import numpy as np; ret=str(np.mean(range(10)))}
</pre>

</body>
</html>
